import express from "express";

const router = express.Router();

router.post("/birds", );
router.get("/birds", );
router.get("/birds/:id", );
router.put("/birds/:id", );
router.delete("/birds/:id", );
router.delete("/birds", );

export default router;