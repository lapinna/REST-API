import express from "express";

const router = express.Router();

router.post("/cats", );
router.get("/cats", );
router.get("/cats/:id", );
router.put("/cats/:id", );
router.delete("/cats/:id", );
router.delete("/cats", );

export default router;