import express from "express";

const router = express.Router();

router.post("/dogs", );
router.get("/dogs", );
router.get("/dogs/:id", );
router.put("/dogs/:id", );
router.delete("/dogs/:id", );
router.delete("/dogs", );

export default router;